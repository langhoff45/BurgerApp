import React from 'react';

const toolbar = () => (
    <header>
        <div>Menu</div>
        <div>Logo</div>
        <nav>
            <ul>
                ...
            </ul>
        </nav>
    </header>
);

export default toolbar;